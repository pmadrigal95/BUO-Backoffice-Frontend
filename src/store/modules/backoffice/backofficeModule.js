import * as shortcut from '@/store/modules/backoffice/modules/shortcut';
import * as dashboard from '@/store/modules/backoffice/modules/dashboard';

const $_modules = {
    shortcut,
    dashboard,
};

export default $_modules;
